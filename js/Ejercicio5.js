
function contarElementos(arr) {
    return arr.length; // Retorna la cantidad de elementos en el arreglo
}

// Ejemplo de uso:
let palabras = ["manzana", "fresa", "pera", "mandarina"];
let cantidad = contarElementos(palabras);
console.log("El arreglo tiene " + cantidad + " elementos.");
